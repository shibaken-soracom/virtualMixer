# .NET build output
bin/
obj/
publish/

# Recording output
*.m4a
*.wav
*.tmp.wav

# Saved recordings (keep the folder, ignore its contents)
recordings/*
!recordings/.gitkeep

# Saved presets (machine-specific device ids; keep folder, ignore contents)
presets/*
!presets/.gitkeep

# Auto-saved session (machine-specific device ids; restored on next launch)
session.json
session.json.tmp
session.json.bak

# IDE / user files
.vs/
*.user
*.suo

# OS
Thumbs.db
desktop.ini
